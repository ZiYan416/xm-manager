<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserMapper">

    <sql id="Base_Column_List">
        id, username, password, name, phone, email, avatar, role, deleted
    </sql>

    <select id="selectAll" resultType="com.example.entity.User">
        select
        <include refid="Base_Column_List" />
        from user
        <where>
            user.deleted=0
            <if test="id != null"> and user.id = #{id}</if>
            <if test="username != null"> and user.username like concat('%', #{username}, '%')</if>
            <if test="password != null"> and user.password = #{password}</if>
            <if test="name != null"> and user.name = #{name}</if>
            <if test="phone != null"> and user.phone = #{phone}</if>
            <if test="email != null"> and user.email = #{email}</if>
            <if test="avatar != null"> and user.avatar = #{avatar}</if>
            <if test="role != null"> and user.role = #{role}</if>
<!--            <if test="deleted != null"> and user.deleted = #{deleted}</if>-->
        </where>
        order by user.id desc
    </select>

    <!-- 物理查询：查询所有记录，不考虑 deleted 字段 -->
    <select id="selectAllPhysical" resultType="com.example.entity.User">
        select
        <include refid="Base_Column_List" />
        from user
        <where>
            <if test="id != null"> and user.id = #{id}</if>
            <if test="username != null"> and user.username like concat('%', #{username}, '%')</if>
            <if test="password != null"> and user.password = #{password}</if>
            <if test="name != null"> and user.name = #{name}</if>
            <if test="phone != null"> and user.phone = #{phone}</if>
            <if test="email != null"> and user.email = #{email}</if>
            <if test="avatar != null"> and user.avatar = #{avatar}</if>
            <if test="role != null"> and user.role = #{role}</if>
        </where>
        order by user.id desc
    </select>

    <select id="selectById" resultType="com.example.entity.User">
        select
        <include refid="Base_Column_List" />
        from user
        where user.id = #{id} and user.deleted = 0
    </select>

    <delete id="deleteById">
        update user
        set deleted = 1
        where id = #{id}
    </delete>

    <insert id="insert" parameterType="com.example.entity.User" useGeneratedKeys="true">
        insert into user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="username != null">username,</if>
            <if test="password != null">password,</if>
            <if test="name != null">name,</if>
            <if test="phone != null">phone,</if>
            <if test="email != null">email,</if>
            <if test="avatar != null">avatar,</if>
            <if test="role != null">role,</if>
            <if test="deleted != null">deleted,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="username != null">#{username},</if>
            <if test="password != null">#{password},</if>
            <if test="name != null">#{name},</if>
            <if test="phone != null">#{phone},</if>
            <if test="email != null">#{email},</if>
            <if test="avatar != null">#{avatar},</if>
            <if test="role != null">#{role},</if>
            <if test="deleted != null">#{deleted},</if>
        </trim>
    </insert>

    <update id="updateById" parameterType="com.example.entity.User">
        update user
        <set>
            <if test="username != null">
                username = #{username},
            </if>
            <if test="password != null">
                password = #{password},
            </if>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="phone != null">
                phone = #{phone},
            </if>
            <if test="email != null">
                email = #{email},
            </if>
            <if test="avatar != null">
                avatar = #{avatar},
            </if>
            <if test="role != null">
                role = #{role},
            </if>
            <if test="deleted != null">
                deleted = #{deleted},
            </if>
        </set>
        where id = #{id}
    </update>

</mapper>
